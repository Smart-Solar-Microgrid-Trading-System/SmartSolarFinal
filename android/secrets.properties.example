# Copy this file to android/secrets.properties and replace the value locally.
# Do not commit android/secrets.properties.
# Use a separate Android key restricted to Maps SDK for Android, the app package,
# and the appropriate signing-certificate SHA-1 fingerprint(s).
MAPS_API_KEY=replace_with_android_maps_key
