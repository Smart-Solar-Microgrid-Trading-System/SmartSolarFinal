<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Required only while the academic LAN/IIS demo uses HTTP rather than HTTPS. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
